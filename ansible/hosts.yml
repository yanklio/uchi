---
all:
  children:
    workstation:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
    server:
      hosts:
        homelab:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
