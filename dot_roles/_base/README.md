# base

- Packages: git, curl, ca-certificates, fzf, ripgrep, gcc, python3, htop, stow, bat, btop, fastfetch
- .gitconfig — user.name, user.email, aliases
- ed25519 SSH key generation
- fastfetch config
