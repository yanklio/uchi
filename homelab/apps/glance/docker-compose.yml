services:
  glance:
    image: ${GLANCE_IMAGE:-docker.io/glanceapp/glance:v0.8.4}
    ports:
      - "${HOMELAB_APP_BIND:-0.0.0.0}:8080:8080"
    volumes:
      - ./glance.yml:/app/config/glance.yml
    restart: unless-stopped
