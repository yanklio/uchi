---
- ansible.builtin.include_tasks: common.yml

- name: "Run all containers"
  ansible.builtin.command:
    argv: [podman, compose, up, -d]
    chdir: "{{ control_homelab_dir }}/apps/{{ item }}"
  loop: "{{ _app_list }}"
