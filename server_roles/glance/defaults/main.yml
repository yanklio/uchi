---
glance_image: docker.io/glanceapp/glance:v0.8.4
glance_port: 8080
glance_backend_port: 18080
glance_bind: 127.0.0.1
glance_nginx_ports:
  - "{{ nginx_listen_port | default('80') }}"
  - 8080
glance_app_dir: "{{ ansible_facts['env']['HOME'] }}/serve"
