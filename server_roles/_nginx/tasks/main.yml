---
- name: Setup nginx
  block:
    - name: Install nginx
      ansible.builtin.package:
        name: nginx
        state: present
      become: true

    - name: Ensure nginx conf.d directory
      ansible.builtin.file:
        path: /etc/nginx/conf.d
        state: directory
        mode: "0755"
      become: true

    - name: Remove legacy combined homelab nginx config
      ansible.builtin.file:
        path: /etc/nginx/conf.d/homelab.conf
        state: absent
      become: true
      notify: reload nginx

    - name: Enable and start nginx
      ansible.builtin.service:
        name: nginx
        enabled: true
        state: started
      become: true
